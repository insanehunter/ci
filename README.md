# ci – a console utility for building with Jenkins

```
Usage:
    ci [-f] JOB_NAME [PARAM=VALUE] - builds job with optional parameters

    Flags:
        -f: force build even if current branch has unpushed commits
```

![Demo Video](/ci-demo.gif)
